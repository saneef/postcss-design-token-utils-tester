# Project to test [postcss-design-token-utils](https://github.com/saneef/postcss-design-token-utils)

1. Checkout this project.
2. Install package `npm install`
3. _Make any change if you need in the config or CSS file._
4. Run `npm run build` to build, and you'll find the generated file, `dist/style.css`
